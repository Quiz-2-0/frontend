import React, { FC } from 'react';
import { FormLayout, FormLayoutGroup } from '@vkontakte/vkui';

const NewQuizStep3: FC = () => {
  const questions = [];
  return (
    <FormLayout>
      <FormLayoutGroup>Шаг 3</FormLayoutGroup>
    </FormLayout>
  );
};

export default NewQuizStep3;
