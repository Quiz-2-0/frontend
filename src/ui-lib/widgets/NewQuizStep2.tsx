import React, { FC } from 'react';
import { FormLayout, FormLayoutGroup } from '@vkontakte/vkui';

const NewQuizStep2: FC = () => {
  const questions = [];
  return (
    <FormLayout>
      <FormLayoutGroup>Шаг 2</FormLayoutGroup>
    </FormLayout>
  );
};

export default NewQuizStep2;
