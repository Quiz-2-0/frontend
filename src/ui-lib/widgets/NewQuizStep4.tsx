import React, { FC } from 'react';
import { FormLayout, FormLayoutGroup } from '@vkontakte/vkui';

const NewQuizStep4: FC = () => {
  const questions = [];
  return (
    <FormLayout>
      <FormLayoutGroup>Шаг 4</FormLayoutGroup>
    </FormLayout>
  );
};

export default NewQuizStep4;
